import RegisterTemplate from "../templates/RegisterTemplate";
import styled from "styled-components";

const Container = styled.div``;

const Register = () => (
  <Container id="registter">
    <RegisterTemplate />
  </Container>
);

export default Register;
